<div class="container">
  <br>
  <h3>ログイン</h3><br>
  <h5><span class="log-in">ユーザー登録済みの方はこちらからログイン</span></h5>
  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="field">
      <%= f.label :メールアドレス %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
    </div>

    <div class="field">
      <%= f.label :パスワード %><br />
      <%= f.password_field :password, autocomplete: "current-password" %>
    </div>
    <br>
    <div class="actions">
      <%= f.submit "ログイン" %>
    </div>
  <% end %>
  <br><br>
  <h5><span class="log-in">ユーザー登録がお済みでない方</span></h5>
  <%= link_to 'こちら', new_user_registration_path %> から新規登録を行ってください。
</div>
